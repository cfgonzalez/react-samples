# Log de instalacion de dependencias:

npm install7
npm install @vitejs/plugin-react -E
npm install react react-dom -E
npm add -D sass 

# Crear archivo vite.config

Confiurar el plugin de react

# Instalar linter

npm install standard -D 